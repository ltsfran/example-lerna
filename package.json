{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "publish:release": "lerna publish --conventional-commits",
    "diff": "lerna diff"
  }
}
